# KunTartib Environment Variables
# Copy this file to .env and fill in your values

# API Configuration
VITE_API_URL=http://localhost:5000/api

# Demo Account (optional - for testing purposes only)
# To enable demo account, set all three values:
# VITE_DEMO_ENABLED=true
# VITE_DEMO_EMAIL=your-demo-email@example.com
# VITE_DEMO_HASH=hashed_password_here

# To generate a hash for your demo password, run in browser console:
# (() => { let h=0; for(let i=0;i<"YOUR_PASSWORD".length;i++){h=((h<<5)-h)+"YOUR_PASSWORD".charCodeAt(i);h=h&h;} return 'hashed_'+Math.abs(h).toString(16); })()
